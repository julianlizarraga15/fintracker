{
  "name": "fintracker",
  "version": "1.0.0",
  "private": true,
  "description": "Personal finance tracker frontend visual checks.",
  "type": "commonjs",
  "scripts": {
    "playwright:install": "npx playwright install --with-deps chromium",
    "visual:check": "node scripts/visual_check.js",
    "visual:check:local": "APP_URL=http://localhost:8080 npm run visual:check"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
